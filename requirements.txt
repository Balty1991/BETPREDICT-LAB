requests>=2.31.0
scipy>=1.12.0
numpy>=1.26.0

# v6.0 ML Engine — adaugate pentru BetPredict Pro v6
scikit-learn>=1.4.0

# Modele de baza ensemble (optionale — fallback automat la sklearn GBM daca lipsesc)
# Instalate implicit pentru acuratete maxima in GitHub Actions
catboost>=1.2.3
lightgbm>=4.3.0
